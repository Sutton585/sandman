services:
  sandman:
    build: .
    container_name: sandman
    volumes:
      # Map the entire project root to /app inside the container
      - .:/app
    # Set the timezone to match your local system (for scheduled runs)
    environment:
      - TZ=America/New_York
    # Run the container and stay alive
    stdin_open: true
    tty: true
